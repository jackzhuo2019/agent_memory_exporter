"""Agent Memory Exporter CLI.

Read-only tool that extracts agent conversation data from local sources
(WorkBuddy, OpenCode), reassembles turns, applies PII redaction, and
exports standardized raw JSON.
"""

__version__ = "0.1.0"
